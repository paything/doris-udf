import type { PivotHeaderLayoutMap } from '../pivot-header-layout';
export declare function getZeroAlignTickAlignTicks(targetRange: {
    min: number;
    max: number;
}, col: number, row: number, index: number, position: 'top' | 'bottom' | 'left' | 'right', layout: PivotHeaderLayoutMap): any[];
export declare function getTickModeFunction(targetTicks: number[], targetRange: {
    min: number;
    max: number;
}, range: {
    min: number;
    max: number;
}, indicatorIndex: number): () => number[];
