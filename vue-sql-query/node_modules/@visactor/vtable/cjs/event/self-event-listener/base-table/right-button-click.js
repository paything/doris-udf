"use strict";

Object.defineProperty(exports, "__esModule", {
    value: !0
}), exports.rightButtonClickEvent = void 0;

const TABLE_EVENT_TYPE_1 = require("../../../core/TABLE_EVENT_TYPE");

function rightButtonClickEvent(table) {
    table.on(TABLE_EVENT_TYPE_1.TABLE_EVENT_TYPE.CONTEXTMENU_CELL, (e => {
        const {col: col, row: row} = e, ranges = table.getSelectedCellRanges();
        let cellInRange = !1;
        if (ranges.length > 0) for (let i = 0; i < ranges.length; i++) {
            const range = ranges[i];
            if (col >= range.start.col && col <= range.end.col && row >= range.start.row && row <= range.end.row) {
                cellInRange = !0;
                break;
            }
        }
        cellInRange || table.selectCell(col, row);
    }));
}

exports.rightButtonClickEvent = rightButtonClickEvent;
//# sourceMappingURL=right-button-click.js.map
